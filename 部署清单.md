# 部署清单 —— 全程点鼠标，不需要打开终端，不需要写代码

这份清单假设你：不写代码、不用终端、只在几个网站的网页界面里点几下鼠标、粘贴两三次文字。全程大约10-15分钟。

**在开始之前**：如果你之前在别的对话里贴过一个Anthropic API key，先去 [console.anthropic.com](https://console.anthropic.com) 把那个key撤销、生成一个新的。下面第4步要用的是这个新key。

---

## 第1步：创建数据库（Supabase）

1. 打开 [supabase.com](https://supabase.com)，注册/登录（免费）。
2. 点 "New Project"，随便起个名字，设一个数据库密码（记下来，等下要用），选一个离你近的区域，点创建。等大约1-2分钟项目初始化完成。
3. 项目建好后，左侧菜单找 **Database → Extensions**，用搜索框找到 `vector`，点击右侧开关启用；再搜 `pg_trgm`，同样启用。这两步就是点两个开关，不用输入任何命令。
4. 左侧菜单找 **Project Settings → Database**，找到 "Connection string" 区域，选 "URI" 格式，复制那一整行（形如 `postgresql://postgres:[YOUR-PASSWORD]@...`）。把其中的`[YOUR-PASSWORD]`替换成你在第2步设的密码。这一整行就是接下来要用的`DATABASE_URL`，先粘贴到一个你自己能找到的地方（比如备忘录），不要发给任何人。

## 第2步：把代码放到GitHub上（不需要git命令）

1. 打开 [github.com](https://github.com)，注册/登录（免费）。
2. 点右上角 "+" → "New repository"，起个名字（比如`public-knowledge-space`），选Private或Public都可以，点创建。
3. 进入这个新仓库页面，点 "uploading an existing file" 这个链接（或者 "Add file" → "Upload files"）。
4. 把我给你的zip包解压后，**把里面所有文件和文件夹**（`server.js`、`worker.js`、`lib/`、`frontend/`、`Dockerfile`、`render.yaml`、`schema.sql`等等，注意不要包含`node_modules`文件夹，如果解压出来有的话就删掉不传）一次性拖进浏览器的上传框里。
5. 滚到底部点 "Commit changes"。全程都是浏览器拖拽上传，不涉及任何命令行。

## 第3步：部署到Render

1. 打开 [render.com](https://render.com)，用GitHub账号直接登录（这样能自动授权访问你刚建的仓库）。
2. 点 "New +" → "Blueprint"。
3. 选择你刚才上传代码的那个GitHub仓库。Render会自动检测到仓库里的`render.yaml`文件，识别出需要创建两个服务（一个网页服务、一个后台worker）。
4. Render会提示你填两个环境变量（因为我在配置文件里特意标注了"不要自动填，让人工手动输入"）：
   - `DATABASE_URL`：粘贴第1步复制的那一整行Supabase连接串。
   - `ANTHROPIC_API_KEY`：粘贴你的真实Claude API key。
   这两个值**只会存在于Render自己的服务器上**，不会经过我、不会出现在GitHub代码里。
5. 点 "Apply" 或 "Create"。Render开始自动构建，大概3-5分钟。

## 第4步：打开你的产品

1. 部署完成后，Render会给"pnks-web"这个服务分配一个网址，形如 `https://pnks-web-xxxx.onrender.com`。
2. 在网址后面加上 `/app/`，也就是访问 `https://pnks-web-xxxx.onrender.com/app/`——这就是你能实际使用的产品页面。
3. 第一次访问时后端会自动建表（代码里写好的自动迁移逻辑），可能要多等几秒。

---

## 如果第3步卡住了：最可能的原因

- **建表失败，日志里提示"extension不存在"**：说明第1步的两个扩展开关没生效或者点漏了，回Supabase的Extensions页面确认`vector`和`pg_trgm`都是"启用"状态。
- **服务起不来，提示连接数据库失败**：检查粘贴的`DATABASE_URL`里是不是还留着`[YOUR-PASSWORD]`占位符没替换成真实密码。
- **免费额度的限制**：Render免费的Web Service在没有访问时会"休眠"，第一次访问可能要等15-30秒才能唤醒，这是免费额度的正常现象，不是故障。

## 诚实的说明

我在自己的沙盒环境里，用本地Postgres+pgvector完整验证过"自动建表"和"用DATABASE_URL连接"这两件事本身是对的（包括故意让两个进程同时抢建表，确认不会撞车）。但我没有办法注册一个真实的Supabase/Render账号去做端到端联调，所以这份清单里"Supabase的按钮长什么样、在哪个菜单"这类细节，是基于这两个平台当前公开的产品设计写的，如果他们改版了界面布局，个别步骤的措辞可能对不上，但整体逻辑（建数据库→开扩展→拿连接串→上传代码→部署→填环境变量）不会变。如果某一步卡住，把Render或Supabase显示的错误信息发给我，我可以继续帮你排查。
